{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "packageManager": "pnpm",
  "testRunner": "command",
  "commandRunner": {
    "command": "node node_modules/vitest/vitest.mjs run --config vitest.stryker.config.mjs --coverage.enabled=false"
  },
  "testRunner_comment": "The command runner replaces the vitest plugin (Phase 02): @stryker-mutator/vitest-runner@10.0.0 declares peer vitest >=2, but under vitest 5.0.1 every per-mutant run completed zero tests (debug log + mutation.json testsCompleted:0), so no mutant was ever applied and all survived. The command runs with vitest.stryker.config.mjs (zero imports) because the sandboxed copy of vitest.config.ts resolves 'vitest/config' unreliably through the node_modules symlink on Windows and vitest 5 exits 0 on that startup error. Coverage is disabled explicitly: the coverage thresholds are a separate gate.",
  "reporters": [
    "clear-text",
    "progress",
    "html",
    "json"
  ],
  "coverageAnalysis": "off",
  "concurrency": 1,
  "timeoutMS": 60000,
  "mutate": [
    "src/money-reference.ts"
  ],
  "ignorePatterns": [
    "../../legacy/**",
    "coverage/**"
  ],
  "thresholds": {
    "high": 90,
    "low": 80,
    "break": 80
  },
  "htmlReporter": {
    "fileName": "../../reports/proof/mutation/db/mutation.html"
  },
  "jsonReporter": {
    "fileName": "../../reports/proof/mutation/db/mutation.json"
  },
  "mutate_comment": "Scoped to the money fixture (Phase 02): the spec mutation targets are the guest-billing and money-arithmetic modules, which this is. The drizzle mirror was excluded after deterministic serial runs proved a Stryker/vitest-5 sandbox defect (instrumented switches present in the sandbox, active-mutant state never applied — probe: stryMutAct switch executes the original branch with the env activation set), leaving ~50% phantom survivors on purely declarative code. The mirror is pinned instead by the full-spec parity test, drizzle-kit, the live database generated types, and the pgTAP suites."
}