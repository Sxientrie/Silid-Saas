# Attacker fixture — the unqualified forbidden term in twelve contexts

# Billing overview

## Billing model

| area | status |
|---|---|
| billing | shipped |

The `billing` module computes totals.

BILLING is out of scope for v1.

Billing is out of scope for v1.

The **billing** module computes totals.

See [billing docs](https://example.invalid) for details.

```
const area = "billing";
```

- The billing module computes totals.

> The bare word "billing" must not appear.

The billing module computes totals.
